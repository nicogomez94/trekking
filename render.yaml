services:
  - type: web
    name: trekking-site
    runtime: static
    plan: free
    buildCommand: "true"
    staticPublishPath: .
    routes:
      - type: redirect
        source: /
        destination: /code.html
